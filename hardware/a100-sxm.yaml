TFlopsPeak: 312
TFlopsFP8: 0
BwPeakTBs: 2.039
MemoryGiB: 80.0
_comment_interconnect: "IntraNodeBwGBps is the on-package NVLink 3 link as a per-GPU NOMINAL unidirectional bandwidth in GB/s: 600 GB/s bidirectional datasheet spec => 300 unidirectional per GPU. vendor_spec (bidirectional / 2), NOT measured effective throughput; any measured correction is a per-cluster blis-registry number. The inter-node fabric figure and the intra:inter ratio it feeds (#1530) live with the reusable Network fabric class (networks/roce-200g.yaml), bound to this chip per deployment by a Cluster (clusters/)."
IntraNodeBwGBps: 300
